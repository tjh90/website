services:
  website-run:
    build:
      context: .
      dockerfile: Dockerfile
      target: website-run
    ports:
      - 8080:8080

  website-nginx:
    build:
      context: .
      dockerfile: Dockerfile-nginx
    ports:
      - 443:443
      - 80:80
