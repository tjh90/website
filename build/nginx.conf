events {
}

http {
    server {
        listen 80;
        listen [::]:80;

        #listen 443 ssl;
        #listen [::]:443 ssl;

        server_name website;
        location / {
            proxy_pass http://website-run:8080;
        }
    }
}
